{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--cdp-endpoint", "http://localhost:9222"
      ],
      "description": "Browser automation for web login and screenshots"
    },
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@anthropic-ai/filesystem-mcp",
        "${workspaceFolder:-.}"
      ],
      "description": "Local filesystem access for reading and writing project files"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": [
        "-y",
        "@anthropic-ai/sequential-thinking-mcp"
      ],
      "description": "Enhanced reasoning for complex multi-step problems"
    }
  }
}
